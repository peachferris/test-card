const checkbox = document.getElementById('check');
let btn = document.querySelector('btn')
const inp = elem.getAttribute(input);


function required () {
    if (checkbox.check , inp.check) {
        btn.disabled = false
    } else {
        btn.disabled = true
    };
}

